#include <stdio.h>
#include <ctype.h>

int main(void)
{
    int i;

    for ( i = 0; i <= 255; i++)
    
        printf("%c ", i);
        
    /* printf("\nTamanio de memoria %ld\n",sizeof(char)); */
}